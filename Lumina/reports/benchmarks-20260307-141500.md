# Benchmarks — 2026-03-07

## Local Ollama Reproducibility Check

Command used:

```powershell
pwsh -ExecutionPolicy Bypass -File ./scripts/benchmark-ollama.ps1 -Model "lumina-core:latest" -Runs 1 -Prompt "State three concise facts about Lumina privacy." -NumCtx 1024 -NumPredict 96 -Temperature 0.1
```

Measured on this machine:

| Metric | Result |
|---|---:|
| First token latency | 55256.56 ms |
| Total latency | 76276.77 ms |
| Prompt eval count | 38 |
| Eval count | 60 |
| Tokens/sec | 2.52 |
| Ollama CPU (% total logical capacity) | 32.83 |
| Ollama working set | 4683.61 MB |

## Delta vs Brutal Ledger Target

- Target: 45 tokens/sec
- Current reproducible: 2.52 tokens/sec
- Gap: -94.4%

## Immediate Interpretation

- Throughput is model/runtime bound on this machine; frontend/packaging work is not the bottleneck.
- First-token latency is extremely high, which aligns with heavy model memory pressure and cold-path loading.
- The app now reports this gap with a reproducible command and exact metrics rather than historical claims.

## Next Optimization Pass (Required)

1. Run quantized variants (`q4_K_M`/`q5_K_M`) and compare tokens/sec with identical prompt settings.
2. Pin model options in backend for short prompts (`num_ctx <= 1024`, reduced `num_predict`) and retest.
3. Validate Ollama runtime flags and GPU availability (`ollama ps`, GPU driver state) before claiming performance targets.
4. Capture first-token latency and tokens/sec across 5 warm runs and store p50/p95 in this folder.

## Additional Thread/Model Sweep (2026-03-07)

| Model | num_thread | num_ctx | num_predict | first token (ms) | total (ms) | tokens/sec |
|---|---:|---:|---:|---:|---:|---:|
| lumina-core:latest | 4 | 1024 | 96 | 102864.51 | 107801.15 | 2.32 |
| qwen2.5:7b | 3 | 1024 | 96 | 92597.72 | 116380.53 | 1.69 |

Interpretation:
- Increasing threads from 3 to 4 did not improve throughput on this machine.
- `lumina-core:latest` remains the best of tested local options, but far from the 45 tok/s target.
- Measured CPU usage dropped unexpectedly in one run, indicating unstable host/runtime scheduling or throttling.

Immediate next action:
- Re-run with 5 warm runs and collect p50/p95 before any further claim updates.
- Validate Ollama runtime health (`ollama ps`, driver state, power mode) and test smaller quantized models.
