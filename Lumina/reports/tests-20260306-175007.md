# Tests Report

Timestamp: 2026-03-06 17:50:07 +03:00
Updated: 2026-03-06 23:40:00 +03:00

## Current State

- Frontend unit tests: Vitest harness active for `src/`
- Frontend E2E tests: Playwright smoke lane active for the desktop web-mode flow
- Backend Rust tests: fast crate lanes active in `src-tauri/crates/lumina-privacy` and `src-tauri/crates/lumina-vault`
- Full backend Rust lib compile/test lane: validated after splitting shared models and engine state out of `src-tauri/src/lib.rs`
- Website tests: none found

## Verification From This Pass

- `npm test`: passed
  - `src/ipc.test.ts`: 4 tests passed
- `npm run test:e2e`: passed
  - `tests/e2e/chat.spec.ts`: 1 test passed
  - Verified typed prompt submission, delayed mocked local inference, and rendered assistant response
- `npm run build` in desktop app: passed after extracting `ChatComposer`
- `npm run build` in website app: passed
- `cargo check -j 1 --manifest-path src-tauri/Cargo.toml`: passed after modularizing the backend shell
- `cargo test` in `src-tauri/crates/lumina-privacy`: passed
  - 3 tests passed
- `cargo test` in `src-tauri/crates/lumina-vault`: passed
  - 3 tests passed
- `cargo test --manifest-path src-tauri/Cargo.toml --lib`: passed
  - 0 runtime tests registered in the top-level Tauri crate, but the lib target compiled and executed cleanly
- `npm run tauri build`: passed through the new app-owned wrapper script
  - Produced `lumina.exe`, MSI, and NSIS installers
  - Confirmed no parked root-level `src-tauri` alias remains after the build completes

## Remaining Test Gaps

1. Add an air-gapped-mode UI test and at least one settings-flow E2E assertion.
2. Add website smoke tests now that the gateway build is green again.
3. Add policy tests for sensitive native commands such as `shell_execute`, `lumina_evolve_app`, and screen/file access.
4. Add a controlled Ollama benchmark harness so performance regressions can be gated from real inference data.

## 2026-03-07 interaction pass

- `npm test`: passed (`9/9`)
- `npm run test:e2e`: passed after tightening selectors for the new chat-title header state
- `cargo check -j 1 --manifest-path src-tauri/Cargo.toml`: passed
- `npm run tauri build`: passed
