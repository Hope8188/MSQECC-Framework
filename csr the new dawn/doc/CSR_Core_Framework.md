# CSR Core Framework

## Status

CSR is the forward framework.

MSQECC is now only the archive label for the earlier programme.

CSR is not a theory of everything and it is not yet a replacement for MOND.
It is the narrow mechanism that survived honest elimination.

## What Is Actually Proved

### 1. `T^3` toric-code backbone

The cubic `T^3` toric code remains the mathematical base.

That includes the standard encoded-qubit result `K = 3` and the stabilizer structure with `X`-type stars and `Z`-type plaquettes.

### 2. Anticommuting bridge

The core bridge result survives.

- commuting measured/coupled pairs do not give the target Lindblad matter channel
- anticommuting measured/coupled pairs do

In the minimal probe calculation, the matter coherence decay is

`Gamma = 2 g^2 / kappa`

### 3. Full-lattice renormalization

The cubic `T^3` extension changes the local rate by a fixed geometry factor.

Under uniform `Z`-plaquette monitoring the local bridge becomes

`d rho_m / dt = (g^2 / (4 kappa)) D[sigma_z](rho_m)`

with

`Gamma_full = g^2 / (2 kappa)`

### 4. Open-systems audit

The surviving bridge and relaxation generators pass the effective GKLS audit at the level currently claimed.

### 5. Code-side relaxation candidate

A second code-side rate is derivable from a damped auxiliary syndrome-reset mode.

`lambda_eff = 2 J^2 / kappa`

### 6. Real hardware bridge realization

A direct IBM hardware bridge test exists and supports the narrow claim that the bridge is physically realizable on real hardware.

### 7. Real hardware relaxation clue

A narrowed IBM experiment exists for the reset-channel branch.

This remains a clue, not a precision extraction.

### 8. Alpha coincidence survives the cheap numerology objection

`4 pi^3 + pi^2 + pi` remains the unique hit below `1e-4` relative error in the tested small-integer family up to coefficient limit `30`.

That keeps the alpha problem alive.

It does not make alpha derived.

## What Is Proved False Or Unsupported

### 1. Current galaxy law as a derived CSR law

That is not established.

### 2. Minimal Hermitian bridge as a direct galaxy kernel

The semiclassical no-go closes this door.

### 3. First tested momentum-carrying channel families as galaxy kernels

The first tested non-Hermitian, retarded, and collective classes produce drag, kicks, or damping rather than a conservative static orbital kernel.

### 4. Earlier local and near-local closure families as final laws

The explicit Jeans closure, constant-dispersion family, disk-tensor family, nonlocal scalar smoothing family, orbital-mixing transition family, and first operator reductions all remain below MOND and/or collapse to family boundaries.

## Operator-Level Branches

### Scalar weighted Hodge surrogate

- SPARC holdout mean `R^2 = 0.7231`
- first real paired THINGS/Leroy aggregate `R^2 = 0.1682`
- fails the structural triple test

### Tensorial Hodge surrogate

- SPARC holdout mean `R^2 = 0.7241`
- first real paired THINGS/Leroy aggregate `R^2 = 0.1707`
- fails the structural triple test

### Retarded Hodge surrogate

- SPARC holdout mean `R^2 = 0.8042`
- first real paired THINGS/Leroy aggregate `R^2 = 0.1182`
- improves failure shape but still fails the aggregate gate

### Scale-invariant constraint template

- SPARC holdout mean `R^2 = 0.9215`
- paired THINGS/Leroy aggregate `R^2 = 0.4170`
- RAR low-acceleration slope `0.598`
- BTFR slope `3.749`
- MDAR low-acceleration slope `-0.402`
- best-fit CSR modulation `eta = 0.00363`

Reading:

This is the first branch to match MOND-level aggregate external performance.

But it does so by imposing the required low-acceleration structure explicitly.
So it is a design constraint template, not a derived CSR law.

## External Data Position

The external target list is now frozen on the THINGS/Leroy branch.

Primary-source benchmark:

- `9 / 11` usable galaxies
- exact Leroy fit form frozen

Sensitivity benchmark:

- `11 / 11` using bounded imputed handles for `n4449` and `n3077`
- useful for robustness, not as the main external claim

The main external message is now sharper:

current operator reductions are still well below MOND unless the low-acceleration structure is imposed directly.

## Constants Side

Two shortcut routes fail cleanly and a third still misses badly.

### Threshold shortcut

Threshold-only weighting is underdetermined.

### Filter shortcut

A naive logical filter fails.

### Richer quotient filter

A richer dual monitored quotient filter improves directionally but still misses alpha badly.

Best richer-filter weights:

- `[0.5381, 0.3334, 0.1285]`

Target:

- `[0.5, 0.25, 0.5]`

So the alpha branch still lacks the right canonical measure.

## Best Current Continuum Direction

The best surviving continuum direction is still the narrower one:

- open monitored abelian BF or Hodge-type continuum completion of the `T^3` CSS backbone
- quotient or Green response on the physical sector space
- canonical measure induced by monitored dynamics rather than threshold or thermal shortcuts

What is not established:

- direct `Z2` BF equals Plebanski gravity
- measurement backaction already generates simplicity constraints
- Immirzi directly determines `alpha`
- `S^1 x T^3` with period `1/kappa` as the canonical partition-function object

## How To Shorten The Search

A fast funnel is now part of the method.

Use cheap gates in order:

1. RAR, BTFR, MDAR triple test
2. SPARC holdout gate
3. paired external aggregate gate
4. paired external median stability gate

Current dispositions:

- `jeans`: kill
- `weighted_hodge`: kill
- `tensorial_hodge`: kill
- `retarded_hodge`: kill in current form
- `scale_invariant_operator`: hold as constraint template

This does not replace physics.

It prevents wasted cycles.

## Exact Bottleneck

The bridge side is now much tighter than the galaxy and constants sides.

The missing object is not another local scalar parameterization.

It is a viable macroscopic response operator and induced measure that turn:

- the proved diffusion source
- the code-side relaxation candidate
- and the surviving hardware clue

into

- a stationary orbital response that is conservative at galaxy scale
- a derived transition scale and low-acceleration bending law
- and a canonical sector measure more structured than threshold or low-dimensional logical filtering

## Bottom Line

CSR is now narrow enough to be scientifically legible.

What survives is:

- a real bridge mechanism
- a real lattice renormalization
- a real open-systems audit
- a real code-side relaxation candidate
- real operator-level conservative surrogates
- a frozen real paired non-SPARC benchmark
- a structural triple test
- a fast pruning funnel
- and one high-value constraint template that shows what the missing derivation has to reproduce
