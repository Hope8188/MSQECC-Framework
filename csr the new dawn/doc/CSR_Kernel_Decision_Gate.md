# CSR Kernel Decision Gate

## Status

Clause 2 is still not triggered.

But the search space is much tighter.

The first branch to clear both the structural triple test and the paired external aggregate gate is the scale-invariant operator family.

That does not mean CSR has derived the law.
It means the missing empirical structure is now explicit.

## Kernel Status

### Weighted Hodge

- SPARC holdout `R^2 = 0.7231`
- paired THINGS/Leroy aggregate `R^2 = 0.1682`
- paired THINGS/Leroy median `R^2 = -8.1047`
- fails the structural triple test

### Tensorial Hodge

- SPARC holdout `R^2 = 0.7241`
- paired THINGS/Leroy aggregate `R^2 = 0.1707`
- paired THINGS/Leroy median `R^2 = -7.5352`
- fails the structural triple test

### Retarded Hodge

- SPARC holdout `R^2 = 0.8042`
- paired THINGS/Leroy aggregate `R^2 = 0.1182`
- paired THINGS/Leroy median `R^2 = -1.2401`
- fails the paired external aggregate gate and the structural triple test

### Scale-Invariant Operator

- SPARC holdout `R^2 = 0.9215`
- paired THINGS/Leroy aggregate `R^2 = 0.4170`
- paired THINGS/Leroy median `R^2 = -1.2556`
- RAR low-acceleration slope `0.598`
- BTFR slope `3.749`
- MDAR low-acceleration slope `-0.402`
- best-fit `eta = 0.00363`

Reading:

This branch works because it imposes the correct low-acceleration asymptotics.
The CSR modulation is weak.
So it is a constraint template, not a derived kernel.

## Constants Side

The alpha branch still has no canonical measure.

Shortcut routes that remain insufficient:

- threshold-only weighting
- naive logical filter
- richer low-dimensional quotient filter

## Decision

The remaining live target is now explicit:

- derive a quotient or Green response with an emergent transition scale
- derive the low-acceleration bending law rather than importing it
- derive a canonical monitored-dynamics measure richer than threshold or low-dimensional logical filtering

## Bottom Line

The gate is sharper now.

The scale-invariant branch proves that the right structure can reach MOND-level aggregate external performance.
What remains unproved is the thing that matters most:

whether CSR can derive that structure from its quotient and measure machinery instead of hard-wiring it.
