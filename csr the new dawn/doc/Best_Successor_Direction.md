# Best Successor Direction After MSQECC

## Executive Judgment

There is not yet a new theory of everything here.

There is, however, a best successor direction.

The best direction is a narrower framework in which:

- the `T^3` toric code remains the mathematical backbone,
- matter couples to the edge operator conjugate to the measured stabilizer basis,
- the required pointer-basis dephasing is generated by syndrome backaction,
- and the galaxy law is rebuilt from a positive-definite response source rather than the current sign-indefinite derivative ansatz.

A compact name for that direction is:

`Conjugate Syndrome Response` (CSR)

This is not a validated theory. It is the best-fitting direction that survives elimination so far.

## Why CSR Survives Elimination

### Eliminated Branch 1: Literal Current MSQECC Galaxy Law

The current global ansatz fails the positive-density audit.

- negative effective density appears in most of the SPARC sample
- no single global positive-density `k` exists across all 171 galaxies

So the current dark-density reading is not physically admissible in its present form.

### Eliminated Branch 2: Commuting Matter Coupling

The literal coupling

`Z_edge tensor sigma_z^matter`

under syndrome measurement does not generate the needed matter dephasing channel.

So that bridge is dead.

### Surviving Branch: Anticommuting Matter Coupling

The coupling

`X_edge tensor sigma_z^matter`

under `Z`-type syndrome measurement does generate the required `sigma_z` dephasing with the exact predicted rate `2 g^2 / kappa` for coherence decay.

That is the first real code-to-matter bridge in the programme.

## Quantum Test

The minimal quantum test is the two-qubit syndrome-channel calculation.

CSR passes that test in the narrow sense that matters most:

- the desired dissipator is not inserted by hand,
- it emerges from stabilizer measurement backaction on the anticommuting branch.

What CSR has not yet passed is a full-lattice matter-channel derivation.

So the quantum standing is:

- minimal bridge test: passed
- full-code derivation: not yet done

## First Positivity-Safe Successor Candidate

A first positivity-safe galaxy candidate was built from the same source kernel that exposed the density problem:

- `u = v_star^2 + k v_gas^2`
- positive source `= max(0, 2u/r + 4u' + r u'')`
- `rho_syn = beta * source`

This candidate is not yet CSR itself. It is only the first positivity-safe phenomenological trial inspired by the bridge results.

### How It Holds Up

On SPARC:

- positivity-safe candidate: `R^2 = 0.8480`
- current MSQECC ansatz: `R^2 = 0.8204`
- MOND: `R^2 = 0.9321`

So it improves materially over the current MSQECC form on the training family.

On the external non-SPARC mass-model sample:

- positivity-safe candidate: `R^2 = 0.5842`
- current MSQECC ansatz: `R^2 = 0.6819`
- MOND: `R^2 = 0.8482`

So it loses external ground relative to the old ansatz.

## What This Means

The positivity-safe candidate is not the new theory.

It is evidence that simply forcing positivity is not enough. A better galaxy law must be derived from the syndrome-response mechanism itself, not from clipping the old kernel by hand.

That is why CSR remains the best successor direction rather than this first candidate law.

## The Real Next Derivation

The next derivation target is now precise:

- derive the macroscopic response kernel produced by repeated syndrome backaction on matter coupled through the conjugate edge operator,
- then compare that derived kernel against the galaxy data under the positive-density requirement.

If that kernel outperforms the current sign-indefinite ansatz externally, the programme has a real successor theory.

If it does not, the toric-code interpretation should be downgraded permanently to a suggestive but unsupported analogy.

## Morphology-Aware Extension

The strongest simple extension of the positivity-safe successor candidate was to let the source amplitude vary by morphology bin.

That raises SPARC aggregate performance to:

- morphology-aware positivity-safe candidate: `R^2 = 0.8649`
- MOND: `R^2 = 0.9321`

So even the best tested positivity-safe extension still does not beat MOND.

This is useful because it kills the easy optimism branch. A better replacement will have to come from a better derived response kernel, not from adding one more empirical coefficient map.
